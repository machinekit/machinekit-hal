USERFUNCTEXAMPLE_DIR := hal/userfunct-example
INCLUDES += $(USERFUNCTEXAMPLE_DIR)

../include/%.h: ./$(USERFUNCTEXAMPLE_DIR)/%.h
	cp $^ $@

$(eval $(call c_comp_build_rules,$(USERFUNCTEXAMPLE_DIR)/ufdemo.o))
